# Environment configuration for testing
#
# IMPORTANT: Use production URL for testing, NOT the test environment
#
# Why we use production URL (https://owc-gemeente.open-regels.nl):
# - Production has correct DMN configurations (config_id=1 -> dish decision)
# - Test environment (.test.) has different/stale database configurations
# - Test environment's config_id=1 points to bijstandsnorm decision, causing hit policy violations
# - We're testing plugin functionality, not environment-specific data
# - Production environment is stable and correctly configured for our test scenarios
#
# DO NOT CHANGE to .test. URL unless you sync the test database configurations first
DMN_TEST_URL=https://owc-gemeente.open-regels.nl

# DMN Engine URL (shared between environments)
DMN_ENGINE_URL=https://operaton-dev.open-regels.nl

# API Key (if needed)
DMN_API_KEY=

# Test environment marker
TEST_ENV=development
